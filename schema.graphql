### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String
  user: User
}

scalar DateTime

type Mutation {
  createDraft(content: String, title: String!): Post
  createSheet(content: String, fileName: String): Sheet
  deletePost(id: Int!): Post
  getSheet(id: Int): Sheet
  login(email: String!, password: String!): AuthPayload
  publish(id: Int!): Post
  signup(email: String!, password: String!): AuthPayload
}

type Post {
  author: User
  content: String
  id: Int
  published: Boolean
  title: String
}

type Query {
  feed: [Post]
  filterPosts(searchString: String): [Post]
  getSheets: [Sheet]
  me: User
  post(id: Int): Post
}

type Sheet {
  content: String
  createdAt: DateTime
  fileName: String
  id: Int
  owner: User
}

type User {
  email: String
  id: Int
  name: String
  posts: [Post!]!
  sheets: [Sheet!]!
}
